alt + Escape
    pkill -USR1 -x sxhkd

alt + Q
    pkill -x swm

alt + Return
    urxvt

alt + {h, j, k, l}
    wmv {-10 0, 0 -10, 0 10, 10 0} $(pfw)

alt + {H, J, K, L}
    wrs {-10 0, 0 -10, 0 10, 10 0} $(pfw)
